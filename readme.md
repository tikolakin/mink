### How to start
```
# have java jdk installed on the host machine 
# have npm installed on the host
npm install -g webdriver-manager
webdriver-manager update
webdriver-manager start
git clone git@github.com:tikolakin/mink.git
cd mink
composer install
php testNavigateToCategory
// observe output
  set up browser and session
  Test passed.
